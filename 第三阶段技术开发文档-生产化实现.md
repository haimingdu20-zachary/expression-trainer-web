# 第三阶段技术开发文档｜生产化实现

## 1. 运行入口

正式桌面应用位于 `表达能力训练MVP/`：

```bash
cd 表达能力训练MVP
npm install
npm run doctor
npm start
```

`npm run doctor` 会检查 Node、Electron、Sherpa-ONNX 和 ASR 模型是否就绪。模型下载说明见 `表达能力训练MVP/models/README.md`。

## 2. 已实现能力

- 六类训练场景与场景对应表达结构；
- AI 出题、本地题目切换和一键带入训练主题；
- 真实麦克风采集和 Sherpa-ONNX 实时中文转写；
- 实时字幕、填充词/犹豫词/笼统词分析；
- 表达密度、语速、停顿次数和最长停顿；
- 本地逻辑引擎阶段判断和下一步提示；
- 结束后的 AI 报告、主动追问、原文复制/保存；
- 第二次训练与第一次训练的逻辑、字数、填充词和语速对比；
- 模型缺失、麦克风拒绝、AI 未配置等错误路径；
- 基于 Node 内置测试运行时的逻辑和词库回归测试。

## 3. 关键数据流

```text
麦克风
  → Renderer Web Audio 16kHz Float32Array
  → preload IPC
  → Main Sherpa-ONNX
  → 实时字幕
  → 词库分析 + logic-engine
  → 左侧指标 / 右侧反馈
  → 停止后生成复盘报告
```

## 4. 验收命令

```bash
cd 表达能力训练MVP
npm test
npm run doctor
```

`npm test` 不需要模型即可验证 JavaScript 语法、逻辑结构和词库分析；`npm run doctor` 在正式启动前检查运行依赖和模型。

## 5. 发布检查清单

- [ ] 安装包内包含正确平台的 Electron 运行文件；
- [ ] 模型文件通过安装器或首次配置流程提供；
- [ ] macOS 麦克风权限正常，录音不会回放造成啸叫；
- [ ] 录音中能看到实时字幕和六项指标；
- [ ] 停止后能生成报告并保存 Markdown；
- [ ] 未配置 AI 时，本地识别、词库和结构提示仍可完成；
- [ ] API 请求失败时不阻塞结束报告的本地数据；
- [ ] 报告模型输出经过转义，不执行模型返回的 HTML；
- [ ] 清理测试 API Key、临时录音和调试日志后再打包。
