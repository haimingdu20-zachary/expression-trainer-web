# 第一阶段技术开发文档｜产品反推与核心交互定义

> 配套文档：[表达能力训练MVP-PRD.md](./表达能力训练MVP-PRD.md)、[技术适配声明-表达能力训练MVP.md](./技术适配声明-表达能力训练MVP.md)
>
> 阶段性质：产品定义与交互设计阶段。
>
> 当前状态：仅输出文档，尚未开始代码开发。

## 一、阶段目标

### 1. 阶段目标

把参考项目中的“表达训练能力”拆解出来，再根据我的 PRD 重组为一套独立的产品方案，明确后续前端页面应该表达什么、用户如何操作、系统在每个状态下反馈什么。

### 2. 阶段产物

- 参考源码能力地图；
- 我的产品与参考项目的差异矩阵；
- 我的产品核心用户流程；
- 六个训练入口与结构的产品定义；
- 训练过程状态模型；
- 前端原型页面范围和页面状态说明；
- 前端 mock 数据契约；
- 下一阶段前端交互页面的验收清单。

### 3. 阶段通过标准

- 不看源码实现细节，也能说明用户为什么使用我的产品；
- 能区分“参考项目已有能力”和“我的产品新增能力”；
- 能从进入训练到查看报告完整描述用户路径；
- 每个页面都有明确输入、输出、空状态、错误状态和结束状态；
- 前端页面可以使用 mock 数据验证核心流程，不依赖 API Key、ASR 模型或后端；
- 产品经理可以根据验收清单判断页面是否符合自己的产品意图。

### 4. 明确不做

- 不写业务代码；
- 不复制 GitHub 源码；
- 不启动 Electron；
- 不进行真实录音和麦克风权限测试；
- 不接入真实 AI；
- 不确定最终前端框架和桌面打包方式；
- 不在这一阶段解决语音识别精度、报告模型质量或部署问题。

## 二、参考项目能力地图

| 参考能力 | 对我的产品的启发 | 我的产品处理 |
| --- | --- | --- |
| 实时语音识别 | 训练过程需要即时看到自己的表达 | 保留“实时字幕”这一产品能力，具体技术后定。 |
| 词库分析 | 能把填充词、犹豫词和笼统词变成即时反馈 | 作为原有能力保留，但不让词库分析成为唯一主角。 |
| AI 实时反馈 | 可以在表达过程中提供教练提示 | 改造成低频、克制的结构化提示；实时结构判断优先由规则完成。 |
| 结束报告 | 给用户一次完整复盘 | 保留并增加训练场景、表达结构、结构缺失、语速、停顿和下一步建议。 |
| 粘贴逐字稿 | 没有录音条件时仍可完成分析 | 作为保留能力和测试入口，不改变实时训练主流程。 |
| 模型设置 | 允许用户选择 AI 后端 | 作为后续配置能力保留，不进入第一阶段前端原型主路径。 |
| 自定义 Prompt | 支持个人训练目标 | 后续再评估是否作为普通用户功能，不把它当作 MVP 核心入口。 |

## 三、我的产品差异矩阵

| 产品维度 | 参考项目倾向 | 我的产品定义 |
| --- | --- | --- |
| 产品定位 | 发现词语问题、获得表达分析 | 实时表达与沟通训练教练，帮助用户知道下一步该表达什么。 |
| 用户入口 | 直接开始录音 | 先选择场景、主题、目标时长和表达结构。 |
| 实时反馈 | 词库提醒和 AI 反馈 | 字幕、词语反馈、当前结构阶段和一条最重要的下一步提示。 |
| 逻辑训练 | 报告中的综合分析 | 训练过程中就显示结论、原因、例子/证据、总结/行动的完成情况。 |
| 结束产物 | 一份分析报告 | 带训练上下文的报告，并支持后续 AI 追问和重录对比。 |
| 训练闭环 | 录音 → 报告 | 选择 → 表达 → 提示 → 报告 → 追问 → 重录 → 对比。 |
| 产品资产 | 原项目品牌和页面 | 独立命名、独立文案、独立视觉和独立前端交互。 |

## 四、核心用户流程

```text
进入产品
  ↓
选择训练场景
  ↓
输入表达主题
  ↓
确认目标时长和表达结构
  ↓
进入训练工作区
  ↓
实时字幕 + 词语问题 + 当前结构进度 + 一条下一步提示
  ↓
结束表达
  ↓
查看训练报告
  ↓
后续阶段：AI 追问 / 重录 / 两次表达对比
```

第一阶段只需要把流程定义到“查看训练报告”，后续追问和重录使用本阶段定义的数据结构。

## 五、六个训练入口定义

| 入口 | 用户任务 | 四段结构 | 实时引导重点 |
| --- | --- | --- | --- |
| 讲清一个想法 | 把一个观点说明白 | 观点 → 原因 → 例子/证据 → 总结 | 先说结论，避免铺垫过长。 |
| 面试回答 | 回答一个面试问题 | 情境/任务 → 行动 → 结果 → 复盘 | 说清个人行动和结果，不只描述背景。 |
| 工作汇报 / 述职 | 汇报进展并说明下一步 | 结论 → 数据/原因 → 风险/案例 → 行动 | 结论先行，行动明确。 |
| 会议发言 / 推动决策 | 针对问题发言并推动决策 | 回应/判断 → 理由/影响 → 方案/依据 → 决策/行动 | 先回应问题，再推动决定。 |
| 说服沟通 / 提案 | 让对方理解并接受主张 | 问题/主张 → 影响/原因 → 方案/证据 → 行动/收益 | 说明对方为什么要接受，以及下一步做什么。 |
| 即兴表达 / 随机回答 | 在短时间内组织回答 | 明确回答 → 两个理由 → 具体例子 → 简短收尾 | 先回答，再展开，最后及时收束。 |

## 六、训练工作区定义

后续前端交互页面必须围绕以下信息组织，而不是复刻参考项目的三栏布局：

### 1. 训练上下文

- 当前场景；
- 当前主题；
- 目标时长；
- 当前表达结构；
- 当前训练状态。

### 2. 表达内容

- 实时字幕；
- 已确认文本与临时识别文本的区别；
- 填充词、犹豫词、笼统词的可理解反馈；
- 录音计时和结束操作。

### 3. 结构导航

- 四个结构阶段的完成状态；
- 当前所在阶段；
- 只展示一条最重要的下一步提示；
- 不用分数取代解释，不在实时过程中展示复杂报告。

### 4. 结束结果

- 本次表达文本；
- 训练场景、主题和结构；
- 结构完整性；
- 语速和停顿指标（有真实数据后展示）；
- 一条最关键的训练建议；
- 生成报告、保存报告和后续训练入口。

## 七、训练状态模型

```text
未开始
  ↓
已配置
  ↓
准备训练
  ↓
表达中 ⇄ 已暂停
  ↓
已结束
  ↓
报告生成中
  ↓
报告完成
```

异常状态：

- 配置不完整：提示补充场景、主题或结构；
- 无法录音：提示麦克风或设备问题；
- 字幕不可用：保留手动粘贴逐字稿入口；
- 报告失败：保留原文和统计数据，允许稍后重试；
- 用户中途退出：下次不显示虚假的“正在录音”。

## 八、前端原型范围

下一阶段制作自己的前端交互页面时，建议只做一条核心路径：

1. 训练配置状态；
2. 训练工作区状态；
3. 结构提示变化状态；
4. 结束后的报告预览状态；
5. 空状态、错误状态和无 AI 状态。

原型阶段使用 mock 数据模拟：

- 字幕逐句出现；
- 词库统计变化；
- 结构阶段从未完成变为完成；
- 下一步提示变化；
- 报告生成完成或失败。

原型阶段不模拟真实 ASR 精度，也不把 mock AI 报告描述成真实模型效果。

## 九、前端 mock 数据契约

```json
{
  "training": {
    "scene": "interview",
    "topic": "如何推动一个跨部门项目",
    "targetDurationSec": 180,
    "structure": ["情境/任务", "行动", "结果", "复盘"]
  },
  "session": {
    "status": "recording",
    "elapsedSec": 42,
    "transcript": "当时项目需要在两周内完成……",
    "stats": {
      "fillers": 2,
      "hedges": 1,
      "vagueWords": 0,
      "words": 86,
      "pauseCount": 2,
      "longestPauseSec": 1.4
    },
    "logic": {
      "completedStages": ["situation_task"],
      "currentStage": "action",
      "hint": "说清你做了什么"
    }
  }
}
```

## 十、Prompt 和 AI 边界

第一阶段只定义边界，不实现 Prompt：

- 实时结构阶段：优先由固定规则判断；
- 词库问题：沿用产品已有词库能力，暂不扩充词库；
- 最终报告：需要接收训练场景、主题、结构和结构分析；
- AI 追问：下一阶段再定义单独 Prompt；
- 模型输出：后续实现时必须有结构校验和错误处理；
- 用户数据：前端原型不得上传真实个人表达内容。

## 十一、验收清单

产品经理后续验收前端原型时，按以下顺序确认：

- [ ] 我能看出这是我自己的表达训练产品，而不是参考项目的换色版本。
- [ ] 我能先选择训练场景、输入主题和确认表达结构。
- [ ] 我能看出训练过程中当前要完成哪个表达阶段。
- [ ] 实时区域只给我一个最重要的下一步动作。
- [ ] 词语反馈不会遮挡或打断主要表达内容。
- [ ] 我能区分未开始、表达中、已暂停、已结束和报告生成中。
- [ ] 没有 AI 时，核心训练流程仍能通过 mock 数据展示。
- [ ] 报告页能看见本次训练的场景、主题、结构和改进建议。
- [ ] 页面中没有出现参考项目的产品名称、品牌文案或无关入口。
- [ ] 关闭或刷新页面后，不会显示虚假的“正在录音”状态。

## 十二、风险与待确认项

### 风险

- 如果过早照搬参考项目页面，产品会回到“词库检测器”，而不是表达结构教练；
- 六个入口的结构提示需要真实用户测试，第一版只能作为可调整的临时规则；
- “实时提示”过多会干扰表达，因此必须坚持一次只展示一个核心提示；
- 语速、停顿和结构得分需要真实数据后再校准，不能在原型阶段制造精确感；
- 参考项目的技术实现不等于我的产品必须采用同一技术。

### 待确认

- 目前无阻塞问题；
- 下一阶段优先制作训练配置页 + 训练工作区 + 报告预览页；
- 最终桌面端技术和真实 ASR 接入，放在前端交互通过验收之后决定。

## 十三、交接给下一阶段

下一阶段可以直接复用：

- 六个训练入口定义；
- 四段式结构模型；
- 训练状态模型；
- 前端页面范围；
- mock 数据契约；
- 非 AI 状态、错误状态和报告状态的验收标准。

下一阶段工作名称建议为：**我的表达训练产品前端交互原型**。它应独立呈现我的产品，不依赖 GitHub 源码和真实模型服务。
