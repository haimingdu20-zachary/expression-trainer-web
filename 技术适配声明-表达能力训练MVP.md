# 技术适配声明｜我的表达训练产品 MVP

> 对应 PRD：[表达能力训练MVP-PRD.md](./表达能力训练MVP-PRD.md)
>
> 参考源码：[fxy2311-youyou/expression-trainer](https://github.com/fxy2311-youyou/expression-trainer)
>
> 本声明的目的，是把 GitHub 参考项目与“我的产品”明确分开。当前阶段只做产品反推和交互定义，不克隆源码、不安装依赖、不启动 APP、不修改 GitHub 项目。

## 1. 产品形态判断

- 产品类型：表达能力训练与沟通教练。
- 核心任务：用户选择训练场景和主题，按照指定表达结构进行表达，获得实时引导和结束后的训练报告。
- 我的产品与参考项目的关系：参考项目提供能力样本和交互启发；我的 PRD 决定产品目标、范围、话术、结构和验收标准。
- 当前开发路径：**产品反推 → 核心交互定义 → 独立前端原型 → 再决定实现方式**。
- 当前阶段：第一阶段是“产品反推与核心交互定义”，不是直接开发桌面 APP。
- 后续候选终端：PRD 中的桌面端仍可保留，但第一阶段先验证自己的产品流程和前端交互，不提前锁死 Electron 实现。

## 2. GitHub 源码的参考边界

参考仓库可观察到的能力包括：

- Electron 主进程、preload 和渲染层组成的本地桌面结构；
- Sherpa-ONNX 本地实时语音识别；
- 实时字幕；
- 填充词、犹豫词、笼统词分析；
- 多模型后端和 AI 实时反馈；
- 结束后的表达分析报告；
- 粘贴逐字稿、设置页和自定义 Prompt 等辅助能力。

这些能力只用于回答“这类产品可以有哪些能力”和“哪些交互值得借鉴”，不等于我的产品必须照搬。

已观察到的参考项目与我的 PRD 之间存在明显差异：

- 参考项目重点是词语精准度和 AI 反馈；我的 PRD 把重点提升为表达结构、实时下一步引导和训练闭环；
- 参考项目没有我的六类训练入口、结构导航、AI 追问和重录对比定义；
- 参考项目 README 描述实时 AI 反馈按 50 字触发，但源码中的前端逻辑使用 30 字作为触发条件；这说明 README 和源码都只能作为观察材料，不能直接成为我的产品规则；
- 参考项目已有视觉和产品命名不属于我的产品资产，不能直接沿用其品牌、页面文案或产品定位。

## 3. 采用的默认方案

| 方案 | 结论 |
| --- | --- |
| PRD 作为产品真相 | 采用。所有功能、优先级和验收均以我的 PRD 为准。 |
| GitHub 源码只读参考 | 采用。只做模块、交互和能力拆解，不把仓库作为我的项目目录。 |
| 产品优先于技术实现 | 采用。先明确用户任务、页面状态和反馈方式，再决定代码和框架。 |
| 前端原型先验证 | 采用。后续前端页面使用 mock 数据验证流程，不先接真实 ASR、模型或密钥。 |
| 实时结构引导优先 | 采用。结构引导是我的核心差异，不被原项目的词库反馈覆盖。 |
| 本地规则与 AI 分层 | 采用。能由固定规则判断的结构阶段不调用 AI；AI 主要用于报告和后续追问。 |
| 渐进式 MVP | 采用。先验证单次训练闭环，再加入追问、重录对比和历史能力。 |

## 4. 触发的按需模块

| 模块 | 触发需求 | 当前决定 |
| --- | --- | --- |
| 源码能力地图 | 需要从参考项目反推出可借鉴能力 | 第一阶段只形成分析文档，不复制代码。 |
| 产品差异矩阵 | PRD 相比参考项目有改动 | 第一阶段必须明确保留、改造、删除和新增。 |
| 前端交互原型 | 用户后续要做自己的前端页面 | 第一阶段定义页面、状态和 mock 数据契约，下一阶段再实现页面。 |
| 结构化训练数据 | 后续报告、追问和重录对比需要共同上下文 | 当前只定义产品数据字段，不引入数据库或真实持久化。 |

## 5. 偏离或暂缓的默认方案

| 默认方案 | 本项目处理方式 | 原因与影响 |
| --- | --- | --- |
| 直接以已有 Electron 项目为基座 | 暂不采用 | 这会把参考项目的技术结构、品牌和交互惯性带入我的产品，削弱产品反推。 |
| 先安装依赖、启动 APP、边跑边改 | 暂不采用 | 当前目标是先得到自己的产品定义和交互，不是复现原项目。 |
| 直接复用原项目页面 | 暂不采用 | 页面应服务我的训练流程、结构导航和报告闭环；原页面只能作为观察样本。 |
| 立即确定 Electron / Web / Tauri 等实现 | 暂缓 | 先验证核心交互；PRD 仍保留桌面端方向，但不在本阶段实施。 |
| Python/FastAPI、数据库、RAG、微调、多 Agent | 暂不引入 | 当前没有远程服务、复杂查询、知识检索或动态工具规划需求。 |
| 真实模型和 API Key | 暂不使用 | 前端原型阶段使用 mock，避免把技术联调误当作产品验收。 |

## 6. 强制底线检查

| 检查项 | 状态 | 说明 |
| --- | --- | --- |
| 密钥与隐私 | 通过 | 当前不接入真实模型、不收集密钥、不上传用户表达内容。 |
| 数据与任务可恢复 | 暂不适用 | 当前只做产品定义和原型契约，没有真实任务和持久化数据。进入实现阶段后重新检查。 |
| 输入输出校验 | 计划处理 | 下一阶段前端 mock 也必须按照产品数据契约处理空状态、错误状态和非法输入。 |
| 错误与日志边界 | 计划处理 | 真实实现阶段不得展示堆栈、密钥或完整敏感原文。 |
| 测试与真实模型验收 | 暂不适用 | 当前没有代码和模型调用；进入实现阶段后执行 mock 测试，再做真实模型冒烟。 |
| 参考项目许可证 | 待后续确认 | 如果未来复制或改造代码，保留原项目 MIT License 和必要版权声明；当前不复制代码。 |

## 7. 第一阶段不做的内容

- 不克隆或改造 GitHub 源码；
- 不生成可运行 APP；
- 不下载 ASR 模型；
- 不安装 Electron、Node 或其他项目依赖；
- 不接入真实模型 API；
- 不新增后端、数据库、RAG、微调或多 Agent；
- 不直接复用参考项目的品牌、命名、视觉和 Prompt；
- 不做最终视觉定稿。

## 8. 需要产品经理决定的问题

当前没有阻塞第一阶段的问题。暂按 PRD 保留以下产品方向：

- 产品最终仍以桌面端为主要候选终端；
- 保留离线语音识别和实时字幕能力；
- 训练场景、表达结构和报告闭环属于核心产品能力。

如果后续希望改成纯 Web 产品、取消本地 ASR 或不再支持桌面端，需要在前端原型验收前单独更新本声明。
