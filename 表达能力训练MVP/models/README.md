# Sherpa-ONNX 模型目录

将与 `lib/asr.js` 中配置匹配的模型放在这里：

```text
models/
└── sherpa-onnx-streaming-paraformer-bilingual-zh-en/
    ├── encoder.int8.onnx
    ├── decoder.int8.onnx
    └── tokens.txt
```

模型文件体积较大，不提交到 Git。请按照项目根目录 README 的说明下载后再启动桌面应用。
