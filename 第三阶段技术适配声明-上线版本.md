# 第三阶段技术适配声明｜上线版本

## 1. 阶段目标

把第二阶段前端交互原型升级为可实际录音、实时转写、实时分析、结束复盘和可配置 AI 的桌面 MVP。第三阶段唯一开发分支为 `phase-3-production`。

## 2. 正式技术路线

- 产品终端：Electron 桌面应用。
- 实时语音识别：Sherpa-ONNX Node，本地 CPU 推理；模型文件不进入 Git。
- 音频采集：渲染进程 `getUserMedia` + Web Audio，采样率 16kHz、单声道。
- 表达分析：本地词库模块，计算笼统词、填充词、犹豫词、表达密度、语速、停顿。
- 结构引导：本地 `logic-engine.js`，不依赖 AI，避免实时提示延迟。
- AI 能力：OpenAI、DeepSeek、Ollama 和自定义 OpenAI 兼容接口；仅用于低频反馈、主动追问和最终报告。
- 数据边界：录音和转写默认保存在当前应用会话；API Key 只写入 Electron 用户目录，不进入代码仓库。

## 3. 与第二阶段原型的关系

第二阶段 `frontend-prototype/` 继续保留为浏览器交互验收原型；第三阶段正式运行入口为 `表达能力训练MVP/`。两者共享 PRD 的六类场景、表达结构和复盘目标，但不再把浏览器 `SpeechRecognition` 作为正式识别方案。

## 4. 上线前置条件

以下内容不能通过提交代码替代：

- 执行 `npm install` 安装 Electron 和 Sherpa-ONNX Node；
- 下载与 `lib/asr.js` 匹配的中英双语 streaming paraformer 模型；
- 如启用云端 AI，在桌面设置页配置用户自己的 API Key；
- 在 macOS、Windows、Linux 分别完成麦克风权限、模型加载、录音、复盘和异常状态验收；
- 生成安装包后再做签名、公证和分发。

## 5. 本阶段不做

- 不把 ASR 模型或 API Key 提交到 Git；
- 不默认上传原始录音；
- 不引入账号、社区、排行榜、RAG 或模型微调；
- 不把 AI 调用放进每一句实时字幕路径。
